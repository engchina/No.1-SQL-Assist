### 分類生成用のPrompt

```

-- 部門テーブル（日本の企業で分かりやすい業務ドメイン）
CREATE TABLE DEPARTMENT (
    DEPARTMENT_ID NUMBER PRIMARY KEY,
    DEPARTMENT_NAME VARCHAR2(50) NOT NULL,
    LOCATION VARCHAR2(50),
    CREATED_AT DATE DEFAULT SYSDATE
);

-- 社員テーブル（社員は部門に所属）
CREATE TABLE EMPLOYEE (
    EMPLOYEE_ID NUMBER PRIMARY KEY,
    DEPARTMENT_ID NUMBER NOT NULL,
    EMPLOYEE_NAME VARCHAR2(100) NOT NULL,
    EMAIL VARCHAR2(100),
    HIRE_DATE DATE DEFAULT SYSDATE,
    SALARY NUMBER(10,2),
    CONSTRAINT FK_EMP_DEPT FOREIGN KEY (DEPARTMENT_ID)
        REFERENCES DEPARTMENT(DEPARTMENT_ID)
);

-- プロジェクトテーブル（プロジェクトは部門が担当）
CREATE TABLE PROJECT (
    PROJECT_ID NUMBER PRIMARY KEY,
    DEPARTMENT_ID NUMBER NOT NULL,
    PROJECT_NAME VARCHAR2(100) NOT NULL,
    START_DATE DATE,
    BUDGET NUMBER(12,2),
    CONSTRAINT FK_PROJ_DEPT FOREIGN KEY (DEPARTMENT_ID)
        REFERENCES DEPARTMENT(DEPARTMENT_ID)
);


-- 社員と部門のビュー
CREATE OR REPLACE VIEW V_EMP_DEPT AS
SELECT e.EMPLOYEE_ID, e.EMPLOYEE_NAME, e.SALARY,
        d.DEPARTMENT_NAME, d.LOCATION
    FROM EMPLOYEE e
    JOIN DEPARTMENT d
    ON e.DEPARTMENT_ID = d.DEPARTMENT_ID;

-- 部門とプロジェクトのビュー
CREATE OR REPLACE VIEW V_DEPT_PROJECT AS
SELECT p.PROJECT_ID, p.PROJECT_NAME, p.BUDGET,
        d.DEPARTMENT_NAME
    FROM PROJECT p
    JOIN DEPARTMENT d
    ON p.DEPARTMENT_ID = d.DEPARTMENT_ID;

INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (10, '総務', '東京');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (20, '経理', '東京');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (30, '人事', '大阪');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (40, '営業', '名古屋');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (50, '開発', '福岡');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (60, 'サポート', '札幌');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (70, '企画', '仙台');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (80, 'マーケティング', '神戸');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (90, '品質保証', '京都');
INSERT INTO DEPARTMENT (DEPARTMENT_ID, DEPARTMENT_NAME, LOCATION) VALUES (100, '法務', '横浜');
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (1, 10, '佐藤 太郎', 'taro.sato@example.com', 420000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (2, 20, '鈴木 花子', 'hanako.suzuki@example.com', 510000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (3, 30, '高橋 健', 'ken.takahashi@example.com', 480000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (4, 40, '田中 美咲', 'misaki.tanaka@example.com', 550000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (5, 50, '伊藤 直樹', 'naoki.ito@example.com', 600000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (6, 50, '渡辺 真央', 'mao.watanabe@example.com', 620000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (7, 40, '山本 大輔', 'daisuke.yamamoto@example.com', 530000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (8, 30, '中村 さくら', 'sakura.nakamura@example.com', 470000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (9, 20, '小林 翔', 'sho.kobayashi@example.com', 520000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (10, 10, '加藤 恵', 'megumi.kato@example.com', 450000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (11, 60, '吉田 光', 'hikari.yoshida@example.com', 400000);
INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, EMAIL, SALARY) VALUES (12, 70, '佐々木 蓮', 'ren.sasaki@example.com', 460000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (101, 50, '受注管理システム', DATE '2025-04-01', 15000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (102, 40, '新製品販売強化', DATE '2025-05-01', 8000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (103, 20, '会計自動化', DATE '2025-02-01', 6000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (104, 10, '社内ポータル刷新', DATE '2025-01-15', 3000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (105, 60, '顧客サポート改善', DATE '2025-03-10', 5000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (106, 70, '市場調査強化', DATE '2025-06-01', 4000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (107, 90, '品質監査強化', DATE '2025-07-01', 3500000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (108, 50, '開発基盤整備', DATE '2025-03-01', 12000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (109, 40, '大口顧客開拓', DATE '2025-04-15', 7000000);
INSERT INTO PROJECT (PROJECT_ID, DEPARTMENT_ID, PROJECT_NAME, START_DATE, BUDGET) VALUES (110, 30, '採用強化', DATE '2025-02-20', 2000000);

=====================================

上記の表構造とデータをもとに、およそ50個の自然言語による質問を作成してください。
これらの質問は、最終的に上記データに対する SQL クエリへ変換されることを想定しています。

また、質問ごとに分類（カテゴリ分け）も行ってください。
分類は自然言語によるラベルとし、最大で 5 種類までとします。
分類によって、どの質問がどのテーブルに関連するか判別できるようにしてください。
```



### 分類の説明

提供された表構造（DEPARTMENT、EMPLOYEE、PROJECT）とビュー（V_EMP_DEPT、V_DEPT_PROJECT）に基づき、生成した問題を以下の5つの分類に分けました。各分類は、自然言語ラベルで示し、問題が主にどのテーブルやビューに焦点を当てているかを区別しています。分類は問題の対象となる主なエンティティに基づいています。

1. **部門関連**: 主にDEPARTMENTテーブルの情報（部門名、場所、作成日など）を対象とした問題。
2. **社員関連**: 主にEMPLOYEEテーブルの情報（社員名、給与、入社日など）を対象とした問題。
3. **プロジェクト関連**: 主にPROJECTテーブルの情報（プロジェクト名、予算、開始日など）を対象とした問題。
4. **社員と部門の組み合わせ**: 主にV_EMP_DEPTビューを使用し、社員と部門の関連情報を対象とした問題。
5. **プロジェクトと部門の組み合わせ**: 主にV_DEPT_PROJECTビューを使用し、プロジェクトと部門の関連情報を対象とした問題。

各問題は自然言語で記述し、それに対応するSQLクエリを記載しています。合計で50個の問題を生成しました。SQLはOracle SQLの構文に基づき、データに基づいて実行可能なものを想定しています。

### 1. 部門関連（10個の問題）
1. 部門テーブルのすべての部門名と場所をリストしてください。  
   SQL: `SELECT DEPARTMENT_NAME, LOCATION FROM DEPARTMENT;`

2. 東京にある部門をすべて教えてください。  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT WHERE LOCATION = '東京';`

3. 部門の作成日が最新の部門は何ですか？  
   SQL: `SELECT DEPARTMENT_NAME, CREATED_AT FROM DEPARTMENT ORDER BY CREATED_AT DESC FETCH FIRST 1 ROW ONLY;`

4. 部門IDが10から50までの部門名を昇順で表示してください。  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT WHERE DEPARTMENT_ID BETWEEN 10 AND 50 ORDER BY DEPARTMENT_ID;`

5. 各場所にある部門の数を教えてください。  
   SQL: `SELECT LOCATION, COUNT(*) AS DEPARTMENT_COUNT FROM DEPARTMENT GROUP BY LOCATION;`

6. 大阪にある部門の名前は何ですか？  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT WHERE LOCATION = '大阪';`

7. 部門IDが偶数の部門をリストしてください。  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT WHERE MOD(DEPARTMENT_ID, 2) = 0;`

8. 作成日が2025年以降の部門はありますか？（注: データはすべてデフォルトでSYSDATEなので、条件なしで全件）  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT WHERE CREATED_AT >= DATE '2025-01-01';`

9. 部門名のアルファベット順で最初の部門は何ですか？  
   SQL: `SELECT DEPARTMENT_NAME FROM DEPARTMENT ORDER BY DEPARTMENT_NAME FETCH FIRST 1 ROW ONLY;`

10. 各部門のIDと名前をIDの昇順で表示してください。  
    SQL: `SELECT DEPARTMENT_ID, DEPARTMENT_NAME FROM DEPARTMENT ORDER BY DEPARTMENT_ID;`

### 2. 社員関連（10個の問題）
1. 社員テーブルのすべての社員名と給与をリストしてください。  
   SQL: `SELECT EMPLOYEE_NAME, SALARY FROM EMPLOYEE;`

2. 給与が500,000円以上の社員を教えてください。  
   SQL: `SELECT EMPLOYEE_NAME, SALARY FROM EMPLOYEE WHERE SALARY >= 500000;`

3. 開発部門（DEPARTMENT_ID=50）に所属する社員の名前をリストしてください。  
   SQL: `SELECT EMPLOYEE_NAME FROM EMPLOYEE WHERE DEPARTMENT_ID = 50;`

4. 入社日が2025年以降の社員は何人いますか？  
   SQL: `SELECT COUNT(*) AS EMPLOYEE_COUNT FROM EMPLOYEE WHERE HIRE_DATE >= DATE '2025-01-01';`

5. 社員の平均給与を計算してください。  
   SQL: `SELECT AVG(SALARY) AS AVG_SALARY FROM EMPLOYEE;`

6. メールアドレスが@example.comで終わる社員の名前を教えてください。  
   SQL: `SELECT EMPLOYEE_NAME FROM EMPLOYEE WHERE EMAIL LIKE '%@example.com';`

7. 給与が最も高い社員の名前と給与は何ですか？  
   SQL: `SELECT EMPLOYEE_NAME, SALARY FROM EMPLOYEE ORDER BY SALARY DESC FETCH FIRST 1 ROW ONLY;`

8. 部門IDが20の社員の数を教えてください。  
   SQL: `SELECT COUNT(*) AS EMPLOYEE_COUNT FROM EMPLOYEE WHERE DEPARTMENT_ID = 20;`

9. 社員IDが奇数の社員の名前をリストしてください。  
   SQL: `SELECT EMPLOYEE_NAME FROM EMPLOYEE WHERE MOD(EMPLOYEE_ID, 2) = 1;`

10. 入社日が最も古い社員の名前は何ですか？  
    SQL: `SELECT EMPLOYEE_NAME FROM EMPLOYEE ORDER BY HIRE_DATE FETCH FIRST 1 ROW ONLY;`

### 3. プロジェクト関連（10個の問題）
1. プロジェクトテーブルのすべてのプロジェクト名と予算をリストしてください。 
   SQL: `SELECT PROJECT_NAME, BUDGET FROM PROJECT;`

2. 予算が1,000万円以上のプロジェクトを教えてください。  
   SQL: `SELECT PROJECT_NAME, BUDGET FROM PROJECT WHERE BUDGET >= 10000000;`

3. 開発部門（DEPARTMENT_ID=50）が担当するプロジェクトの名前をリストしてください。  
   SQL: `SELECT PROJECT_NAME FROM PROJECT WHERE DEPARTMENT_ID = 50;`

4. 開始日が2025年4月以降のプロジェクトは何ですか？  
   SQL: `SELECT PROJECT_NAME, START_DATE FROM PROJECT WHERE START_DATE >= DATE '2025-04-01';`

5. プロジェクトの平均予算を計算してください。  
   SQL: `SELECT AVG(BUDGET) AS AVG_BUDGET FROM PROJECT;`

6. プロジェクトIDが100以上のプロジェクトの名前を教えてください。  
   SQL: `SELECT PROJECT_NAME FROM PROJECT WHERE PROJECT_ID >= 100;`

7. 予算が最も低いプロジェクトの名前と予算は何ですか？  
   SQL: `SELECT PROJECT_NAME, BUDGET FROM PROJECT ORDER BY BUDGET FETCH FIRST 1 ROW ONLY;`

8. 部門IDが40のプロジェクトの数を教えてください。  
   SQL: `SELECT COUNT(*) AS PROJECT_COUNT FROM PROJECT WHERE DEPARTMENT_ID = 40;`

9. プロジェクト名のアルファベット順で最初のプロジェクトは何ですか？  
   SQL: `SELECT PROJECT_NAME FROM PROJECT ORDER BY PROJECT_NAME FETCH FIRST 1 ROW ONLY;`

10. 開始日が2025年1月のプロジェクトをリストしてください。  
    SQL: `SELECT PROJECT_NAME, START_DATE FROM PROJECT WHERE START_DATE >= DATE '2025-01-01' AND START_DATE < DATE '2025-02-01';`

### 4. 社員と部門の組み合わせ（10個の問題）
1. V_EMP_DEPTビューを使って、すべての社員名と所属部門名をリストしてください。  
   SQL: `SELECT EMPLOYEE_NAME, DEPARTMENT_NAME FROM V_EMP_DEPT;`

2. 東京にある部門の社員の名前と給与を教えてください。  
   SQL: `SELECT EMPLOYEE_NAME, SALARY FROM V_EMP_DEPT WHERE LOCATION = '東京';`

3. 各部門の平均給与を計算してください。  
   SQL: `SELECT DEPARTMENT_NAME, AVG(SALARY) AS AVG_SALARY FROM V_EMP_DEPT GROUP BY DEPARTMENT_NAME;`

4. 給与が550,000円以上の社員とその部門名をリストしてください。  
   SQL: `SELECT EMPLOYEE_NAME, DEPARTMENT_NAME FROM V_EMP_DEPT WHERE SALARY >= 550000;`

5. 開発部門の社員の名前を昇順で表示してください。  
   SQL: `SELECT EMPLOYEE_NAME FROM V_EMP_DEPT WHERE DEPARTMENT_NAME = '開発' ORDER BY EMPLOYEE_NAME;`

6. 各場所の社員数を教えてください。  
   SQL: `SELECT LOCATION, COUNT(*) AS EMPLOYEE_COUNT FROM V_EMP_DEPT GROUP BY LOCATION;`

7. 給与が最も高い社員の名前と部門名は何ですか？  
   SQL: `SELECT EMPLOYEE_NAME, DEPARTMENT_NAME FROM V_EMP_DEPT ORDER BY SALARY DESC FETCH FIRST 1 ROW ONLY;`

8. 大阪にある部門の社員の平均給与を計算してください。  
   SQL: `SELECT AVG(SALARY) AS AVG_SALARY FROM V_EMP_DEPT WHERE LOCATION = '大阪';`

9. 社員名が「佐藤」で始まる社員の部門名を教えてください。  
   SQL: `SELECT DEPARTMENT_NAME FROM V_EMP_DEPT WHERE EMPLOYEE_NAME LIKE '佐藤%';`

10. 各部門の社員数を部門名順で表示してください。  
    SQL: `SELECT DEPARTMENT_NAME, COUNT(*) AS EMPLOYEE_COUNT FROM V_EMP_DEPT GROUP BY DEPARTMENT_NAME ORDER BY DEPARTMENT_NAME;`

### 5. プロジェクトと部門の組み合わせ（10個の問題）
1. V_DEPT_PROJECTビューを使って、すべてのプロジェクト名と担当部門名をリストしてください。  
   SQL: `SELECT PROJECT_NAME, DEPARTMENT_NAME FROM V_DEPT_PROJECT;`

2. 開発部門が担当するプロジェクトの名前と予算を教えてください。  
   SQL: `SELECT PROJECT_NAME, BUDGET FROM V_DEPT_PROJECT WHERE DEPARTMENT_NAME = '開発';`

3. 各部門のプロジェクト数を計算してください。  
   SQL: `SELECT DEPARTMENT_NAME, COUNT(*) AS PROJECT_COUNT FROM V_DEPT_PROJECT GROUP BY DEPARTMENT_NAME;`

4. 予算が5,000万円以上のプロジェクトとその部門名をリストしてください。  
   SQL: `SELECT PROJECT_NAME, DEPARTMENT_NAME FROM V_DEPT_PROJECT WHERE BUDGET >= 5000000;`

5. プロジェクト名が「新製品」で始まるプロジェクトの部門名を教えてください。  
   SQL: `SELECT DEPARTMENT_NAME FROM V_DEPT_PROJECT WHERE PROJECT_NAME LIKE '新製品%';`

6. 各部門のプロジェクトの合計予算を計算してください。  
   SQL: `SELECT DEPARTMENT_NAME, SUM(BUDGET) AS TOTAL_BUDGET FROM V_DEPT_PROJECT GROUP BY DEPARTMENT_NAME;`

7. 予算が最も高いプロジェクトの名前と部門名は何ですか？  
   SQL: `SELECT PROJECT_NAME, DEPARTMENT_NAME FROM V_DEPT_PROJECT ORDER BY BUDGET DESC FETCH FIRST 1 ROW ONLY;`

8. 営業部門のプロジェクトの平均予算を計算してください。  
   SQL: `SELECT AVG(BUDGET) AS AVG_BUDGET FROM V_DEPT_PROJECT WHERE DEPARTMENT_NAME = '営業';`

9. プロジェクトの開始日が2025年3月以降のものを部門名とともにリストしてください。  
   SQL: `SELECT PROJECT_NAME, DEPARTMENT_NAME FROM V_DEPT_PROJECT WHERE START_DATE >= DATE '2025-03-01';`

10. 各部門のプロジェクト数を予算の降順で表示してください。  
    SQL: `SELECT DEPARTMENT_NAME, COUNT(*) AS PROJECT_COUNT FROM V_DEPT_PROJECT GROUP BY DEPARTMENT_NAME ORDER BY COUNT(*) DESC;`